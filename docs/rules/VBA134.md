# VBA134 — ParamArray element type is not Variant

**Severity:** 🔴 error    **Category:** `signature`    **Phase:** 2.10

## Description

A `ParamArray` must be declared as an array of `Variant`.

## Failing example

```vb
Sub S(ParamArray a() As Long): End Sub
```

## Compliant example

```vb
Sub S(ParamArray a() As Variant): End Sub
```

## How to fix

Declare the ParamArray as `ParamArray name() As Variant`.

---

_Source: [src/rules.py](https://github.com/twobeass/VBAlidator/blob/main/src/rules.py) — entry `VBA134`._
