# VBA142 — Named arguments with ParamArray

**Severity:** 🔴 error    **Category:** `signature`    **Phase:** 2.9

## Description

A procedure declaring a `ParamArray` cannot be called using named arguments.

## Failing example

```vb
Sub S(ParamArray a() As Variant)
End Sub
Sub T()
    Call S(a:=1)
End Sub
```

## Compliant example

```vb
Sub S(ParamArray a() As Variant)
End Sub
Sub T()
    Call S(1, 2, 3)
End Sub
```

## How to fix

Pass arguments positionally when the procedure has a ParamArray.

---

_Source: [src/rules.py](https://github.com/twobeass/VBAlidator/blob/main/src/rules.py) — entry `VBA142`._
